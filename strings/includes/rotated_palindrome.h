#ifndef __ROTATED_PALINDROME_H__
#define __ROTATED_PALINDROME_H__

#include <string>

namespace valhalla {
    namespace strings {
        namespace rotated_palindrome {
            class Solution {
            public:
                bool exists(std::string&);
            };
        }
    }
}

#endif